local config = {
  lsp = {
    formatting = {
      -- control auto formatting on save
      format_on_save = {
        enabled = false, -- enable or disable format on save globally
      },
    },
  },

  --cmp = {
  --  tags = 1000,
  --  nvim_lsp = 900,
  --  luasnip = 750,
  --  path = 500,
  --  buffer = 250,
  --}
}
return config
